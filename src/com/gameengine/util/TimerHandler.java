package com.gameengine.util;

/**
 * This handler it's used to execute some acctions when timer neads
 */
public interface TimerHandler {

	/**
	 * This method will called when the timer updates
	 */
	void update();
	
}
